#   Uses my dotfiles to load stuff

#   Load Git stuff
if [ -f $DOTFILES/git/.aliases ]; then
   source $DOTFILES/git/.aliases
fi

# Extra stuff specific for the machine
if [ -f ~/.extra ]; then
   source ~/.extra
fi
