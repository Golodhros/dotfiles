# Create the powerline shell configuration file if there isn't any
mkdir ~/.config/powerline-shell

# Create symbolic links to the powerline-shell config
ln -s ~/.dotfiles/powerline-shell/config.json ~/.config/powerline-shell/config.json

