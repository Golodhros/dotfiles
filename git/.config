# Avoid less to paginate
# Useful to avoid pages when doing git diff
# Reference: https://stackoverflow.com/questions/2183900/how-do-i-prevent-git-diff-from-using-a-pager
export LESS="-F -X $LESS"

# Run only once
# eval "$(git config --global pager.branch false)"
