# Copy to ~/.gitconfig.local and adjust per machine. Not tracked beyond this example.
# install.sh copies it into place automatically if ~/.gitconfig.local is missing.
[user]
	name = Marcos Iglesias
	email = golodhros@gmail.com
